# LenaLink Backend - Environment Variables Example
# Copy this file to .env and update values for your environment

# ==================== SERVER CONFIGURATION ====================
# Server host to bind to
SERVER_HOST=localhost

# Server port to listen on
SERVER_PORT=8080

# HTTP read timeout (duration format: 10s, 1m, etc.)
SERVER_READ_TIMEOUT=15s

# HTTP write timeout (duration format: 10s, 1m, etc.)
SERVER_WRITE_TIMEOUT=15s

# Server shutdown timeout
SERVER_SHUTDOWN_TIMEOUT=30s

# ==================== DATABASE CONFIGURATION ====================
# Database driver to use
# Options: memory (default), postgres, sqlite
DB_DRIVER=memory

# Database host (for postgres and sqlite)
DB_HOST=localhost

# Database port (for postgres)
DB_PORT=5432

# Database username (for postgres)
DB_USER=postgres

# Database password (for postgres)
DB_PASSWORD=

# Database name
DB_NAME=lenalink

# SSL mode for PostgreSQL
# Options: disable, require, verify-ca, verify-full
DB_SSLMODE=disable

# Maximum number of database connections in pool
DB_MAX_CONNECTIONS=25

# Minimum number of database connections in pool
DB_MIN_CONNECTIONS=5

# Connection TTL (time-to-live)
DB_CONNECTION_TTL=5m

# Query timeout
DB_QUERY_TIMEOUT=30s

# Path to database migrations
DB_MIGRATIONS_PATH=./migrations

# ==================== LOGGING CONFIGURATION ====================
# Log level
# Options: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Use JSON format for logs (false for text format)
LOG_JSON_FORMAT=false

# ==================== EXAMPLE POSTGRESQL SETUP ====================
# For production, use PostgreSQL:
#
# DB_DRIVER=postgres
# DB_HOST=prod-db.example.com
# DB_PORT=5432
# DB_USER=lenalink_user
# DB_PASSWORD=secure_password_here
# DB_NAME=lenalink_prod
# DB_SSLMODE=require
# LOG_LEVEL=WARN
#
# Then run migrations:
# migrate -path migrations -database "postgres://user:pass@host:5432/db" up

# ==================== DEVELOPMENT SETUP ====================
# For development with in-memory storage (no database needed):
#
# DB_DRIVER=memory
# LOG_LEVEL=DEBUG
# SERVER_PORT=3000
#
# Then run:
# go run ./cmd/server
